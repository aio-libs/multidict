Sped up construction, insertion and lookups in the C extension by
keeping the operations behind Python slots and the rare table
compaction out of line, so they stopped crowding the hot paths out of
the compiler's inlining budget -- by :user:`asvetlov`.
