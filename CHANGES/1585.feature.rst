Changed the report of a failing watcher callback to name the event and
the multidict's type and address, as CPython does for dict watchers,
and stopped passing the multidict itself to :func:`sys.unraisablehook`,
whose ``object`` became ``None`` -- by :user:`asvetlov`.
