Rewrote the benchmark scripts around a single registry of benchmarked
operations, and added a Callgrind driver that records deterministic
instruction counts per operation. Destructive operations such as
``pop()`` and ``clear()`` are now measured without counting the cost of
rebuilding the mapping -- by :user:`asvetlov`.
