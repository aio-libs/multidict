Fixed a segmentation fault in the C extension when ``del md[key]`` or
:meth:`~multidict.MultiDict.popall` released a value whose finalizer let
another thread resize the same multidict while the deletion was still
scanning the table -- by :user:`asvetlov`.
